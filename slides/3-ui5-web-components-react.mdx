import { Button, Calendar, Label } from "@ui5/webcomponents-react";

<!-- section-title: UI5 Web Components for React -->

![](../assets/UI5WCR_Logo.png)

<br />
<br />
<br />

<!-- fragments-start -->

- provides Wrapper Components for all UI5 Web Components
- provides complex layouts (e.g. `ObjectPage`) and charts built on top of UI5 Web Components
- Type Definitions for all components

<!-- fragments-end -->

---

# Wrapper Components

Our Wrapper components are hiding the complexity of dealing with Custom Elements in React and let you use the UI5 Web Components
in React as if they were "regular" React Components.

---

<!-- classes: extendedMaxWidth -->

## Boolean Prop Handling

<!-- block-start: grid -->

<!-- block-start: column,fadedOut -->

###### with plain UI5 Web Components

```jsx
import "@ui5/webcomponents/dist/Button";

function MyComponent() {
  const booleanPropsBtn1 = {};
  const booleanPropsBtn2 = {};

  const button1ShouldBeDisabled = false;
  const button2ShouldBeDisabled = true;

  if (button1ShouldBeDisabled === true) {
    booleanPropsBtn1.disabled = true;
  }
  if (button2ShouldBeDisabled === true) {
    booleanPropsBtn2.disabled = true;
  }

  return (
    <>
      <ui5-button
        onClick={() => alert("I'll be there onClick!")}
        {...booleanPropsBtn1}
      >
        Click me!
      </ui5-button>

      <ui5-button
        onClick={() => alert("I will never show up!")}
        {...booleanPropsBtn2}
      >
        But you can't click me!
      </ui5-button>
    </>
  );
}
```

<!-- block-end -->
<!-- block-start: column -->

###### with UI5 Web Components for React

```jsx
import { Button } from "@ui5/webcomponents-react";

function MyComponent() {

  return (
    <>
      <Button
          onClick={() => alert("I'll be there onClick!")}
          disabled={false}
      >
        Click me!
      </Button>
      <Button
          onClick={() => alert("I will never show up!")}
          disabled={true}
      >
        But you can't click me!
      </Button>
    </>
  );
}
```

<!-- block-end -->
<!-- block-end -->

<div className="scaledFlexBox">
  <Button
      onClick={() => alert("I'll be there onClick!")}
      disabled={false}
  >
    Click me!
  </Button>
  <Button
      onClick={() => alert("I will never show up!")}
      disabled={true}
  >
    But you can't click me!
  </Button>
</div>

---

<!-- classes: extendedMaxWidth -->

## Event Handlers

<!-- block-start: grid -->

<!-- block-start: column,fadedOut -->

###### with plain UI5 Web Components

```jsx
import "@ui5/webcomponents/dist/Calendar";

function MyComponent() {
  const ref = useRef();

  useEffect(() => {
    const currentRef = ref.current;
    const handler = (e) => {
      e.detail.dates.forEach((d) => {
        alert(new Date(d * 1000));
      });
    };

    // bind the event handler after the component has mounted
    if (currentRef) {
      currentRef.addEventListener("selected-dates-change", handler);
    }

    // remove the event handler before component unmounts to free up memory
    return () => {
      if (currentRef) {
        currentRef.removeEventListener("selected-dates-change", handler);
      }
    };
  }, []);

  return <ui5-calendar ref={ref} />;
}
```

<!-- block-end -->
<!-- block-start:column -->

###### with UI5 Web Components for React

```jsx
import { Calendar } from "@ui5/webcomponents-react";

function MyComponent() {
  return (
    <Calendar
      onSelectedDatesChange={(e) => {
        e.detail.dates.forEach((d) => alert(new Date(d * 1000)));
      }}
    />
  );
}
```

<br />
<br />
<br />
<br />

<div className="scaledFlexBox">
  <Calendar
    onSelectedDatesChange={(e) => {
      e.detail.dates.forEach((d) => alert(new Date(d * 1000)));
    }}
  />
</div>

<!-- block-end -->
<!-- block-end -->

---

<!-- classes: extendedMaxWidth -->

## Support for standard HTML props

<!-- block-start: grid -->
<!-- block-start: column,fadedOut -->

###### with plain UI5 Web Components

```jsx
import "@ui5/webcomponents/dist/Label";

function MyComponent() {
  return <ui5-label class="textColorRed">I'm a text!</ui5-label>;
}
```

<!-- block-end -->
<!-- block-start: column -->

###### with UI5 Web Components for React

```jsx
import { Label } from "@ui5/webcomponents-react";

function MyComponent() {
  return <Label className="textColorRed">I'm a text!</Label>;
}
```

<!-- block-end -->
<!-- block-end -->



<br />
<Label className="textColorRed" style={{ fontSize: "24px" }}>
  I'm a text!
</Label>

<br />
<br />
<br />
<br />

In addition to fixing the `className` issue, our components are also passing through all `aria-`, `data-` and event handler
attributes.

---

# Complex Layouts and Charts

<iframe style={{width: 'var(--base-max-width)'}} height={800} src="https://sap.github.io/ui5-webcomponents-react/master/iframe.html?id=layouts-floorplans-objectpage--default-story&viewMode=story&sap-ui-theme=sap_fiori_3_dark" />

---

# Complex Layouts and Charts

<iframe style={{width: 'var(--base-max-width)'}} height={800} src="https://sap.github.io/ui5-webcomponents-react/master/iframe.html?id=charts-barchart--default-story&viewMode=story&sap-ui-theme=sap_fiori_3_dark" />

---

# Type Definitions

<!-- block-start: grid -->
<!-- block-start: column -->

![](../assets/ButtonPropTypes.png)

<!-- block-end -->

<!-- block-start: column -->

![](../assets/ButtonTypeAhead.png)

<!-- block-end -->
<!-- block-end -->

---

<!-- classes: demo -->

<!-- block-start: attribution -->

<!--

[Source](http://gph.is/1LmxrD8)

-->

<!-- block-end -->

<!-- section-title: Demo -->

# Demo

---

# Thank you!

## Links:

- [Github Repository](https://github.com/SAP/ui5-webcomponents-react)
- [Documentation](https://sap.github.io/ui5-webcomponents-react)
- [Tutorial](https://developers.sap.com/mission.react-spa.html)

<br />
<br />
<br />

## Contact:

<!-- block-start: grid -->
<!-- block-start: column -->

###### Marcus Notheis

<br />

marcus.notheis@sap.com

<!-- block-start: social -->

[![Github](../assets/GitHub-Mark-Light-32px.png)](https://github.com/MarcusNotheis)

[![Twitter](../assets/Twitter.png)](https://twitter.com/marcusnotheis)

<!-- block-end -->

<!-- block-end -->

<!-- block-start: column -->

###### Lukas Harbarth

<br />

lukas.harbarth@sap.com

<!-- block-start: social -->

[![Github](../assets/GitHub-Mark-Light-32px.png)](https://github.com/Lukas742)

[![Twitter](../assets/Twitter.png)](https://twitter.com/luk742)

<!-- block-end -->

<!-- block-end -->
<!-- block-end -->
